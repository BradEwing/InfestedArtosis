package unit.managed;

import bwapi.Game;
import bwapi.Unit;

public class InfestedTerran extends ManagedUnit {
    public InfestedTerran(Game game, Unit unit, UnitRole role) {
        super(game, unit, role);
    }
}
