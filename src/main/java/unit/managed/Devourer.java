package unit.managed;

import bwapi.Game;
import bwapi.Unit;
import info.map.GameMap;

public class Devourer extends ManagedUnit {
    public Devourer(Game game, Unit unit, UnitRole role, GameMap gameMap) {
        super(game, unit, role, gameMap);
    }
}
