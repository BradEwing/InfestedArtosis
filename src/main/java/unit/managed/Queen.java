package unit.managed;

import bwapi.Game;
import bwapi.Unit;
import info.map.GameMap;

public class Queen extends ManagedUnit {
    public Queen(Game game, Unit unit, UnitRole role, GameMap gameMap) {
        super(game, unit, role, gameMap);
    }
}
