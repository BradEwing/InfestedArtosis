package unit.managed;

import bwapi.Game;
import bwapi.Unit;
import info.GameState;

public class Ultralisk extends ManagedUnit {
    public Ultralisk(Game game, Unit unit, UnitRole role, GameState gameState) {
        super(game, unit, role, gameState);
    }
}
