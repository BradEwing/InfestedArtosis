package unit.managed;

import bwapi.Game;
import bwapi.Unit;
import info.GameState;

public class Broodling extends ManagedUnit {
    public Broodling(Game game, Unit unit, UnitRole role, GameState gameState) {
        super(game, unit, role, gameState);
    }
}
