package unit.managed;

import bwapi.Game;
import bwapi.Unit;

public class Defiler extends ManagedUnit {
    public Defiler(Game game, Unit unit, UnitRole role) {
        super(game, unit, role);
    }
}
