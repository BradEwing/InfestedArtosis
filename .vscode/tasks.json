{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Move JSON and CSV files",
            "type": "shell",
            "command": "powershell",
            "args": [
                "-ExecutionPolicy", "ByPass",
                "-File", "${workspaceFolder}/scripts/move-files.ps1"
            ],
            "problemMatcher": []
        }
    ]
}
